import { SignupForm } from "@/components/auth/signup-form";

/**
 * Signup page.
 * Per FR-001: System MUST provide a signup page accessible at /signup path.
 */
export default function SignupPage() {
  return <SignupForm />;
}
