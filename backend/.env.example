# Database connection (Neon PostgreSQL with transaction pooling)
# Format: postgresql+asyncpg://{user}:{password}@{host}:6432/{database}?sslmode=require
DATABASE_URL=postgresql+asyncpg://user:password@host:6432/database?sslmode=require

# Better Auth shared secret (must match frontend BETTER_AUTH_SECRET)
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# Environment
ENVIRONMENT=development
