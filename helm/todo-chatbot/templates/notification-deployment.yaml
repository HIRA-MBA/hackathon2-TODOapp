{{- if .Values.notification.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-chatbot.fullname" . }}-notification
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
    app.kubernetes.io/component: notification
spec:
  replicas: {{ .Values.notification.replicaCount }}
  selector:
    matchLabels:
      {{- include "todo-chatbot.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: notification
  {{- if .Values.notification.strategy }}
  strategy:
    {{- toYaml .Values.notification.strategy | nindent 4 }}
  {{- end }}
  template:
    metadata:
      labels:
        {{- include "todo-chatbot.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: notification
      {{- if .Values.dapr.enabled }}
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "notification-service"
        dapr.io/app-port: "{{ .Values.notification.service.targetPort }}"
        dapr.io/app-protocol: "http"
        dapr.io/enable-metrics: "true"
      {{- end }}
    spec:
      containers:
        - name: notification
          image: "{{ .Values.global.registry | default "" }}{{ if .Values.global.registry }}/{{ end }}{{ .Values.notification.image.repository }}:{{ .Values.notification.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.notification.service.targetPort }}
              protocol: TCP
          env:
            - name: PORT
              value: "{{ .Values.notification.service.targetPort }}"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "todo-chatbot.secretName" . }}
                  key: DATABASE_URL
            - name: BACKEND_URL
              value: "http://{{ include "todo-chatbot.fullname" . }}-backend:{{ .Values.backend.service.port }}"
            {{- if .Values.dapr.enabled }}
            - name: DAPR_HTTP_PORT
              value: "3500"
            {{- end }}
          resources:
            {{- toYaml .Values.notification.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: {{ .Values.notification.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.notification.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.notification.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.notification.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.notification.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.notification.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.notification.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.notification.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.notification.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.notification.probes.readiness.failureThreshold }}
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
{{- end }}
